#name: Deploy showcase-app to GitHub Pages
#
#on:
#  push:
#    branches: [main]
#  workflow_dispatch:
#
#permissions:
#  contents: read
#  pages: write
#  id-token: write
#
#concurrency:
#  group: pages
#  cancel-in-progress: true
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#
#    env:
#      SITE_BASE_URL: https://example.com
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Setup Node
#        uses: actions/setup-node@v4
#        with:
#          node-version: 22
#          cache: npm
#
#      - name: Install dependencies
#        run: npm ci
#
#      - name: Build showcase-app
#        run: npm run build:showcase-app
#
#      - name: Prevent Jekyll processing
#        run: touch dist/showcase-app/browser/.nojekyll
#
#      - name: SPA fallback for unknown routes
#        run: cp dist/showcase-app/browser/index.csr.html dist/showcase-app/browser/404.html
#
#      - name: Upload Pages artifact
#        uses: actions/upload-pages-artifact@v3
#        with:
#          path: dist/showcase-app/browser
#
#  deploy:
#    needs: build
#    runs-on: ubuntu-latest
#
#    environment:
#      name: github-pages
#      url: ${{ steps.deployment.outputs.page_url }}
#
#    steps:
#      - name: Deploy to GitHub Pages
#        id: deployment
#        uses: actions/deploy-pages@v4
